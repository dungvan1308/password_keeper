<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nvdung.passwordkeeper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Khai bao tab Activity -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" /> <!-- Main actity chay dau tien -->

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SumActivity">
            <!-- Chi can khai bao Activity vi ko phai Main nen ko can khai bao Laucher -->
        </activity>

        <activity android:name=".ListActivity">
            <!-- Chi can khai bao Activity vi ko phai Main nen ko can khai bao Laucher -->
        </activity>

    </application>

</manifest>